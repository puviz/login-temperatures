import React from 'react';

export default function ApplicationLogo({ className }) {
    return (
        <p className={`text-blue-600/75 font-bold ${className}`}>
            Ltemp
        </p>
    );
}
